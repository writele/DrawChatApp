@page "/"
@inject NavigationManager NavigationManager
@inject Microsoft.JSInterop.IJSRuntime js;
@implements IAsyncDisposable;
@using DrawChatApp.Components;


<div class="form-group">
    <label>
        User Name:
        <input @bind="userNameInput" />
    </label>
        <label>
        Room Name:
        <input @bind="roomNameInput" />
    </label>
</div>

<button disabled="@(!IsNewGameEnabled())" @onclick="CreateGame">Create New Game</button>

